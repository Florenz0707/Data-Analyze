# M0 可复现基线报告

> 采集日期：2026-08-28
> 基线提交：`6dedd5fcfefdf2ec5b6cff1035a9260882486a7a`
> 状态：自动化基线与项目角色口径的双评审已完成；10 条样本存在待裁决差异，用户侧结构化评分尚未补齐，因此 M0 尚未通过最终验收。

## 1. 基线范围

本报告固定以下变量：

- 前端 `127.0.0.1:8082`，后端 `127.0.0.1:8081`，Ollama `127.0.0.1:11434`；
- LLM：`deepseek-r1:7b`，7.6B、Q4_K_M，digest `755ced02...3140383f`；
- Embedding：`bge-large:latest`，334.09M、F16、1024 维，digest `b3d71c92...f4021c15`；
- 检索 Top-K：10；生成重试 5 次，最小输出 100 字符，每段最多 5 条、每条最多 70 字符；
- 5 个 CSV，共 1708 条日志；Chroma Collection 为 `log_collection_bge_large_latest`，向量数 1708；
- 评测集 50 条：Python、Java、Linux 命令、Windows 事件、通用服务各 10 条，其中 10 条 Windows 事件为知识库无证据负样本。

详细环境、模型 digest、文件大小和 SHA-256 见 [`evidence/environment_manifest.json`](./evidence/environment_manifest.json)。

## 2. 运行环境

| 项目            | 基线值                                          |
| --------------- | ----------------------------------------------- |
| OS              | WSL2 Linux 6.6.87.2，x86_64                     |
| CPU             | Intel Core i7-13700H，20 逻辑 CPU               |
| 内存 / Swap     | 15.5 GiB / 4 GiB                                |
| GPU             | WSL 内未检测到；Ollama 的实际宿主加速状态未采集 |
| Python          | 3.13.11                                         |
| Node.js / npm   | 20.20.2 / 10.9.8                                |
| uv / pre-commit | 0.9.24 / 4.5.1                                  |
| Ollama          | 0.13.5                                          |

Python 版本由 `backend/django_backend/.python-version` 固定，前端版本由 `.nvmrc`、`packageManager` 和 `engines` 固定。

## 3. 数据与索引清单

| 文件                             | 数据行 |   字节 | SHA-256             |
| -------------------------------- | -----: | -----: | ------------------- |
| `20200.csv`                      |    570 | 55,375 | `2cc4e21a...62f499` |
| `java_error_deepseek.csv`        |    274 | 38,273 | `dee646dd...302f28` |
| `linuxcomand_error_deepseek.csv` |    365 | 37,104 | `ba86c33c...c667c9` |
| `python_error_deepseek.csv`      |    409 | 38,698 | `42bec354...fbb79`  |
| `python_error_doubao.csv`        |     90 | 17,302 | `be42b770...01d609` |

Chroma 逻辑文档 SHA-256 为 `168f9cbe9c57d9944a8e78dc9c831f9805369d359bdff7737ed7e24a026d8fb2`。SQLite 文件哈希可能因 Chroma 维护写入发生变化，回归时以 Collection 名称、向量数、维度和逻辑文档哈希为稳定口径。

## 4. 评测集质量

| 指标          |                                        实测 |  目标 | 结果                   |
| ------------- | ------------------------------------------: | ----: | ---------------------- |
| 查询数        |                                          50 |  ≥ 50 | 通过                   |
| 分类覆盖      |                             5/5，每类 10 条 |  5 类 | 通过                   |
| 负样本        |                                10/50（20%） | ≥ 20% | 通过                   |
| 字段完整率    |                               50/50（100%） |  100% | 通过                   |
| 有效日志 ID   |                                       40/40 |  100% | 通过                   |
| 疑似密钥/私钥 |                                           0 |     0 | 通过                   |
| 双人独立标注  | 10/50（20%，项目角色口径；实际执行者含 AI） | ≥ 20% | **通过（冲突待裁决）** |

验证输出由 `validate_dataset.py` 生成。用户已审核全部 10 条负样本并确认无问题；按用户指定，Codex 作为第二评审角色对同批样本完成独立复核，逐条记录原因正确性、证据一致性、步骤可执行性、禁用建议和拒答情况。两者结论存在待裁决差异：用户未提供结构化分数，第二评审角色发现多条无证据诊断。详细结果见 [`evidence/double_review_results.json`](./evidence/double_review_results.json)。项目角色口径的双评审覆盖达到 20%；证据同时保留实际执行者为 AI，不能据此对外宣称两名真人评审。

## 5. 检索质量基线

正样本 40 条；负样本不进入排序指标，因为当前检索器没有拒答阈值并始终返回 Top-K。

| 指标      | 两次运行均值 | 两次最大差异 |
| --------- | -----------: | -----------: |
| Recall@1  |       0.8000 |       0.0000 |
| Recall@5  |       0.9500 |       0.0000 |
| Recall@10 |       0.9500 |       0.0000 |
| MRR@10    |       0.8646 |       0.0000 |
| NDCG@10   |       0.9337 |       0.0000 |

未在 Top-10 命中的样本：

- `S004`：密码复杂度校验失败；
- `S006`：Swap 使用率达到 85%。

完整排名、距离和日志 ID 见 [`evidence/retrieval_baseline.json`](./evidence/retrieval_baseline.json)。

## 6. 生成与 API 基线

全量 50 条查询均使用独立会话和关闭历史模式执行；回答正文未写入 API 指标报告，只记录长度和 SHA-256。完整合成回答另存于人工复核包。

| 指标                         |                              实测 |
| ---------------------------- | --------------------------------: |
| HTTP 成功率                  |                     50/50（100%） |
| 失败率 / 空回答率            |                           0% / 0% |
| 五段回答结构有效率           |                              100% |
| Chat 最小 / P50 / P95 / 最大 | 11.70s / 17.17s / 25.83s / 39.67s |
| 同问题缓存命中               |             19.82ms，回答哈希一致 |
| 后端 OpenAPI 热请求 P95      |                           13.63ms |
| 前端首页热请求 P95           |                            2.12ms |
| 负样本明确拒答标记率         |                    **0/10（0%）** |

负样本 0% 明确拒答说明当前系统没有基于检索证据的 abstention 机制。返回结构完整不能替代证据正确性，这一问题必须在 RAG 质量阶段修复并回归。

完整指标见 [`evidence/api_baseline.json`](./evidence/api_baseline.json)。第二评审角色的原因正确性、证据一致性和步骤可执行性评分已记录；用户侧尚未提供可比的结构化评分。

## 7. 启动与索引性能

| 项目                        |           实测 | 条件                                                                     |
| --------------------------- | -------------: | ------------------------------------------------------------------------ |
| 服务进程启动到 OpenAPI 就绪 |          4.88s | 独立 18081 进程，触发 AppConfig 预加载；Ollama daemon/model cache 未清空 |
| 服务进程峰值 RSS            |    782,384 KiB | 同上                                                                     |
| 1708 条索引临时重建总耗时   |         28.06s | batch=32，`bge-large:latest`                                             |
| 其中 Embedding / Chroma add | 25.82s / 2.07s | 临时目录，完成后删除                                                     |
| 索引构建进程峰值 RSS        |    177,876 KiB | 增量峰值 63,652 KiB                                                      |

由于没有停止现有 Ollama daemon 或卸载模型，本次 4.88s 是“新 Django 进程 + 已存在模型服务”的可复现启动基线，不是机器重启后的端到端冷启动。

## 8. 已知限制与后续动作

1. 由用户裁决当前 10 条负样本的结论差异，补充用户侧结构化评分并保留双方原始记录；
2. 当前索引无源文件/行号 metadata，后续重建索引时原生写入稳定日志 ID；
3. 引入证据阈值和无证据拒答策略，将负样本正确拒答率纳入强制指标；
4. 当前第二评审角色评分已记录；若形成生产级质量声明，仍需真实第二名人类完成原因正确性、证据一致性和步骤可执行性评分；
5. 如需“机器级冷启动”，应在独立维护窗口停止 Ollama、清理模型常驻状态后另行测量，不在本次运行中干扰用户已启动的服务。

## 9. 数据清理

API 评测使用 `m0_evaluation` 和 `m0_evaluation_full` 两个合成账号。回答导出后，账号、Token、会话与历史已从本地 SQLite 删除，清理结果见 [`evidence/cleanup.json`](./evidence/cleanup.json)。
